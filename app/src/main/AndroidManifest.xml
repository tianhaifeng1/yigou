<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xzte">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".DateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lifecycle.TestLifecycleActivity">

        </activity>
        <activity android:name=".bottomtab.BottomTabActivity" />
        <activity
            android:name=".test3.TwoActivity"
            android:theme="@style/dialog" />
        <activity android:name=".test3.OneActivity"></activity>
        <activity android:name=".test2.TestToBackActivity" />
        <activity android:name=".test2.TestBackActivity" />
        <activity android:name=".greendao.TestGreenDaoActivity" />
        <activity android:name=".TestFilterModuleActivity" />
        <activity android:name=".TestNestRecyclerActivity" />
        <activity android:name=".TextActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".TestFlexboxLayoutActivity" />
    </application>

</manifest>